작업환경 : Linux

컴파일
 : 20161563.h , 20161563.c , makefile. 세가지의 파일은 동일한 디렉토리에 위치시킨다.
이후 명령어 make를 입력한다.
make로 컴파일 후 수정하지 않고 다시 make를 입력할 경우 실행되지 않는다. 명령어 make clean을 먼저 입력한 뒤 make를 입력해주어야 한다.

실행순서
 : Linux 환경에서 명령어 tar -xvf sp20161563_proj1.tar을 입력하여 압축을 해제한다. 
명령어 make를 입력하면 실행 파일 20161563.out이 생성된다.
./20161563.out을 입력해 프로그램을 실행한다.
